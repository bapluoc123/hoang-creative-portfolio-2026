# Ghi chú cập nhật — 11/08/2026

## Đã hoàn thành

- Cập nhật nội dung, vai trò, tiêu đề và ảnh bìa cho các dự án chính; đồng bộ ảnh bìa ở trang danh sách và trang chi tiết.
- Bổ sung dự án MUSAIC và đưa dự án vào đúng thứ tự trong danh sách.
- Hoàn thiện bộ ảnh ABEECI, AURORADOT và Little Red Riding Hood, gồm cover, character, flower, emotion và element atlas.
- Thay logo bằng bộ PNG sáng/tối, dùng chung cho header, loading và các section nền tối.
- Xóa các badge/card phụ, dòng OUTPUT, text phụ và những hình/section bị lặp theo yêu cầu.
- Tinh gọn trang ELIHIGH KIDS: bổ sung vai trò Illustrator và bỏ các section không còn sử dụng.
- Đồng bộ hiệu ứng hover của project card và liên kết email `hoangbh511999@gmail.com`.
- Sửa hệ responsive chung:
  - giới hạn vùng nội dung lớn ở 1800px để màn hình 2K/4K không bị kéo giãn;
  - bổ sung breakpoint tablet 1024px và tối ưu mobile 760px/640px;
  - đồng bộ cover, gallery, grid, khoảng cách và typography;
  - sửa cắt chữ ABEECI/AURORADOT và tràn mockup Fix Now trên mobile;
  - giới hạn chiều cao gallery trên màn hình lớn.
- Chuyển font Be Vietnam Pro, Noto Serif và Roboto Mono sang self-host trong `public/fonts`, loại bỏ đường dẫn font tuyệt đối của Windows trong bản build Cloudflare.

## Đã kiểm tra

- Next.js production build: đạt.
- Vinext/Cloudflare production build: đạt.
- Bản build không còn đường dẫn `C:/Users/.../.vinext/fonts`.
- Kiểm tra trực quan ở 2560px, 1024px và 390px cho trang chủ cùng các trang ELIHIGH KIDS, MOOD, AURORADOT, FIX NOW, ABEECI và MUSAIC.
- Không phát hiện tràn ngang ở các kích thước đã kiểm tra.

## Còn dang dở / cần làm tiếp

- Chưa deploy bản responsive và font mới lên Cloudflare Pages.
- Bộ test `tests/rendered-html.test.mjs` đang lỗi thời: vẫn tìm `codex-preview` và `app/_sites-preview/SkeletonPreview.tsx`, trong khi cấu trúc đó không còn trong website hiện tại.
- ESLint hiện còn các lỗi cũ về dùng thẻ `<a>` cho route nội bộ và cảnh báo dùng `<img>`; build production vẫn thành công nhưng nên xử lý trong một lượt refactor riêng.
- Nên thực hiện thêm một vòng kiểm tra thủ công toàn bộ các section sâu và animation dài trước lần deploy chính thức tiếp theo.

## Trạng thái phát hành

- Mã nguồn được chuẩn bị trên branch `agent/portfolio-visual-content-updates`.
- Cloudflare Pages giữ nguyên bản đang chạy cho đến khi có yêu cầu deploy mới.
