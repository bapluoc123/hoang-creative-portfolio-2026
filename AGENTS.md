# Portfolio project instructions

Before changing this portfolio, read `CONTINUE_NEXT_WEEK.md` completely.

- Preserve the existing dirty worktree and do not overwrite unrelated changes.
- Treat the local website as the current source of truth.
- Keep Vietnamese typography and diacritics intact.
- For ABEECI and Fix Now, use authentic project assets instead of generic replacements.
- Run `npm.cmd run build` after meaningful code changes.
- Never store access tokens or credentials in this repository.

